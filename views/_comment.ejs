<li id="comment-<%= comment.id %>">
    <p>
        <%= comment.content %> <br>
            <small>
                <%= comment.user.name %>
            </small>
    </p>
    <% if(locals.user && (locals.user.id==comment.user.id || locals.user.id==post.user.id)){ %>
        <a class="comment-delete-btn" href="/comments/delete/<%= comment.id %>">X</a>
        <% } %>
</li>